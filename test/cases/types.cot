struct CtPoint {
    x: i64,
    y: i64
}

type CtInt = i64
type CtCoord = CtPoint

test "type alias" {
    var x: CtInt = 42
    @assertEq(x, 42)
}

test "struct alias" {
    var pt = CtCoord { .x = 10, .y = 20 }
    @assertEq(pt.x + pt.y, 30)
}
