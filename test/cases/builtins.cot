struct CbPoint {
    x: i64,
    y: i64
}

test "sizeof basic" {
    @assertEq(@sizeOf(i64), 8)
}

test "sizeof struct" {
    @assertEq(@sizeOf(CbPoint), 16)
}

test "alignof basic" {
    @assertEq(@alignOf(i64), 8)
}

test "intcast basic" {
    var big: i64 = 42
    var small = @intCast(i32, big)
    @assertEq(small, 42)
}
