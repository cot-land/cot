const CeColor = enum {
    Red,
    Green,
    Blue
}

const CeStatus = enum {
    Ok = 0,
    Warning = 50,
    Error = 100
}

test "enum simple" {
    var c = CeColor.Green
    @assertEq(c, 1)
}

test "enum explicit value" {
    var s = CeStatus.Error
    @assertEq(s, 100)
}
