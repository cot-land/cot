test "array simple" {
    var arr = [42]
    @assertEq(arr[0], 42)
}

test "sum two" {
    var arr = [1, 2]
    @assertEq(arr[0] + arr[1], 3)
}

test "sum three" {
    var arr = [1, 2, 3]
    @assertEq(arr[0] + arr[1] + arr[2], 6)
}

test "index access" {
    var arr = [10, 20, 30, 40]
    @assertEq(arr[2], 30)
}

test "update" {
    var arr = [10, 20, 30]
    arr[1] = 100
    @assertEq(arr[1], 100)
}

test "append one" {
    var arr = [1, 2, 3]
    var arr2 = append(arr, 4)
    @assertEq(len(arr2), 4)
}
