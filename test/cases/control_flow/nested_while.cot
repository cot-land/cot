// EXPECT: exit_code=6
// Counts i*j combinations where i<3 and j<2: 0*0,0*1,1*0,1*1,2*0,2*1 = 6 iterations
fn main() int {
    let i: int = 0;
    let count: int = 0;
    while (i < 3) {
        let j: int = 0;
        while (j < 2) {
            count = count + 1;
            j = j + 1;
        }
        i = i + 1;
    }
    return count;
}
