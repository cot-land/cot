test "and" {
    @assertEq(255 & 15, 15)
}

test "or" {
    @assertEq(240 | 15 - 200, 55)
}

test "xor" {
    @assertEq((255 ^ 15) - 200 + 200, 240)
}

test "shl" {
    @assertEq(1 << 4, 16)
}

test "shr" {
    @assertEq(64 >> 2, 16)
}

test "not" {
    @assertEq(~0 & 255, 255)
}
