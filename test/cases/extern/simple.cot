// Extern function declaration - becomes wasm import
// EXPECT: exit_code=42

// Declare an extern function (imported from host)
// This generates: (import "env" "console_log" (func (type 0)))
extern fn console_log(ptr: i64, len: i64);

fn main() i64 {
    // We don't call it here since we can't run JS imports in test
    // This test verifies the wasm import section is generated correctly
    return 42
}
