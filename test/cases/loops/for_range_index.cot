// For-range loop with index binding: for i, x in arr
// EXPECT: exit_code=10
// Sum of indices 0+1+2+3+4 = 10

fn main() i64 {
    var arr = [10, 20, 30, 40, 50]
    var sum: i64 = 0
    for i, x in arr {
        sum = sum + i
    }
    return sum
}
