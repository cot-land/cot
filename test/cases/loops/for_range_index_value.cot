// For-range loop using both index and value
// EXPECT: exit_code=63
// sum = (0+10) + (1+20) + (2+30) = 10 + 21 + 32 = 63

fn main() i64 {
    var arr = [10, 20, 30]
    var sum: i64 = 0
    for i, x in arr {
        sum = sum + i + x
    }
    return sum
}
