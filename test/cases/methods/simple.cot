// Simple method call
// EXPECT: exit_code=30

struct Point {
    x: i64,
    y: i64,
}

impl Point {
    fn sum(self: *Point) i64 {
        return self.x + self.y
    }
}

fn main() i64 {
    let p = Point { .x = 10, .y = 20 }
    return p.sum()
}
