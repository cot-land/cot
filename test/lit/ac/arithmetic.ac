// RUN: %cot emit-cir %s | %FileCheck %s

fn math(a: i32, b: i32) -> i32 {
    return a + b - a * b / a % b
}

// CHECK-LABEL: func.func @math
// CHECK: cir.add
// CHECK: cir.mul
// CHECK: cir.div
// CHECK: cir.rem
// CHECK: cir.sub
