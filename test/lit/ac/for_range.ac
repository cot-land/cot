// RUN: %cot emit-cir %s | %FileCheck %s

fn sum_range() -> i32 {
    var total: i32 = 0
    for i in 0..10 {
        total += i
    }
    return total
}

// CHECK-LABEL: func.func @sum_range
// CHECK: cir.alloca i32 : !cir.ptr
// CHECK: cir.alloca i32 : !cir.ptr
// CHECK: cir.br
// CHECK: cir.cmp slt
// CHECK: cir.condbr
// CHECK: cir.add
// CHECK: cir.store
// CHECK: cir.add
// CHECK: cir.store
// CHECK: cir.br
