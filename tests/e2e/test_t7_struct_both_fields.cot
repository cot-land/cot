// expect: 142
// Tier 7: Access both struct fields via params
struct Point { x: i64, y: i64 }

fn getX(p: Point) i64 { return p.x }
fn getY(p: Point) i64 { return p.y }

fn main() i64 {
    let p = Point { .x = 42, .y = 100 }
    return getX(p) + getY(p)
}
